学习笔记
课程：制造智能技术作业：基于 AI 工具的制造智能应用开发记录内容：AI 工具学习、拟选用的 harness 与模型、Git 原理与功能、选题调研

一、对 AI 工具的学习
在本次课程作业中，我学习并实践了使用 AI 工具辅助完成从选题、方案设计到应用开发的全过程。主要学习与体会如下：
1.1 使用的 AI 工具
工具
类型
主要用途
学习体会
豆包（Doubao）办公模式
AI Agent 助手
选题调研、方案撰写、全栈应用生成
自然语言即可驱动复杂工程任务，适合快速完成原型
豆包妙搭 / 应用构建能力
全栈应用生成
一键生成前端 UI、后端 API、数据库与业务逻辑
把需求描述清楚比手写代码更关键，prompt 质量决定产物质量
通用网络搜索
信息检索
调研公开数据集（如 NASA C-MAPSS）、算法原理
用于核实文献出处与数据集来源，避免凭印象编造

1.2 学习收获
AI 不是替代思考，而是放大思考：选题、技术方向、验收标准仍需自己判断；AI 负责把方案快速落地为可运行代码。
需求描述决定产物质量：用结构化方式描述业务流程、输入输出、验收标准，AI 生成的应用才能走通完整流程，而不是空壳页面。
需要人工验证：AI 生成的代码与数据仍需检查是否真实可运行（模型是否真在推理、数据是否合理），不能默认 "看起来对"。
Vibe Coding 方法：以自然语言描述意图 → AI 生成代码 → 人工测试反馈 → 迭代修改，这种 "对话式编程" 显著降低了全栈开发的门槛。

二、拟选用的 harness 和模型
2.1 概念理解
Harness（运行宿主 / 智能体框架）：指承载 AI Agent 运行循环的软件环境，负责把用户输入、工具调用、文件读写、网页生成等环节串联起来。它决定了 Agent 能调用哪些工具、产物落到哪里。
模型（Model）：底层的大语言模型，负责理解意图、生成代码与文本推理。
2.2 本次作业选用
项目
选择
说明
Harness
豆包（Doubao）办公模式
具备全栈应用生成、文件读写、命令执行、网页预览等能力，适合本课程 "方案 + 可运行 demo" 的交付要求
底层模型
豆包大模型
由 Harness 自动调度，负责需求理解、代码生成、算法逻辑实现
应用生成通道
全栈应用（fullstack）生成
同时生成前端页面、后端 API 与数据库，满足 B/S 架构要求

2.3 选择理由
课程要求交付 "完整 B/S 可运行 demo"，需要同时产出前端、后端、数据库，单靠纯前端工具无法满足；
全栈应用生成通道内置服务端与数据库能力，可承载业务逻辑与算法推理；
豆包办公模式支持自然语言迭代修改，便于在评审后快速调整页面与功能。

三、Git 原理与功能
3.1 Git 是什么
Git 是一个分布式版本控制系统，用于跟踪文件变化、协同开发、回溯历史版本。与集中式版本控制（如 SVN）不同，Git 每个开发者本地都有完整仓库副本。
3.2 核心原理
（1）快照而非差异
Git 存储的不是文件 "前后差异"，而是每次提交时对全部文件做一次快照（snapshot）。若文件未修改，Git 只保留指向之前已存储版本的指针，而非重复存储。
（2）三大区域
工作区（Working Directory）   ←  你看到的、正在编辑的文件
       │ git add
       ▼
暂存区（Staging Area / Index）←  下次提交的清单
       │ git commit
       ▼
仓库区（.git Repository）      ←  永久历史快照

（3）对象模型
Git 仓库由四类对象组成，均通过 SHA-1 哈希寻址：
Blob：文件内容；
Tree：目录，记录文件名与 Blob/Tree 指针；
Commit：一次提交，包含指向 Tree、父提交、作者、时间、说明的指针；
Tag：对某次提交的具名标记（如 v1.0）。
（4）分支原理
分支本质上是一个指向某次 Commit 的可移动指针。新建分支只是新建一个指针，切换分支只是移动指针、更新工作区，因此极其轻量。HEAD 是当前所在分支 / 提交的指针。
3.3 常用功能与命令
场景
命令
作用
初始化仓库
git init
在当前目录创建 .git
查看状态
git status
查看工作区 / 暂存区改动
暂存改动
git add <文件> / git add .
把改动放入暂存区
提交
git commit -m "说明"
把暂存区快照存入仓库
查看历史
git log
查看提交历史
创建 / 切换分支
git branch <名> / git checkout -b <名>
新建并切换分支
合并分支
git merge <分支>
把指定分支合并到当前分支
关联远程
git remote add origin <url>
绑定远程仓库
推送
git push -u origin <分支>
本地提交推送到远程
拉取
git pull
拉取远程更新并合并
回退
git reset --hard <commit>
回退到指定版本

3.4 协同工作流程
典型流程：clone 远程仓库 → 新建功能分支 → 提交改动 → push 分支 → 发起 Pull/Merge Request → 评审后合并主干。这种方式保证主干稳定、多人并行不冲突。

四、选题调研
4.1 调研方向
围绕 "制造 / 工业环节智能化应用"，我调研了以下方向：
方向
代表技术
数据可得性
与课程章节契合度
机器视觉缺陷检测
CNN、图像分类
需大量缺陷图像，公开样本少
主要对应视觉一章，覆盖面窄
设备预测性维护
LSTM、RUL 预测
NASA C-MAPSS 公开数据集成熟
对应深度学习 / 预测性维护
工艺质量预测
树模型、回归
工艺数据可模拟
对应机器学习章节
生产调度优化
启发式规则、智能算法
工单数据可模拟
对应生产调度章节
工业大数据分析
数据清洗、特征工程
传感器数据可模拟
对应大数据章节

4.2 公开数据集调研
NASA C-MAPSS（Turbofan Engine Degradation Simulation Data Set）：NASA 发布的经典预测性维护公开数据集，记录多台涡扇发动机在多传感器下从全新到失效的时序退化数据，提供剩余寿命（RUL）标签，被广泛用于 LSTM/RUL 建模研究。本项目以此作为预测性维护算法的建模范式参考。
工艺 / 质检数据：由于课程不要求真实企业数据，依据切削加工领域常识（转速、进给量、切深、温度与缺陷率的经验关系）合理模拟带标签数据集。
4.3 最终选题
综合调研结果，选定 "精密制造车间智能质量管控与预测性维护决策支持平台"，理由：
场景真实：覆盖生产、质量控制、设备运维、生产管理多个制造环节；
技术覆盖广：整合工业大数据、机器学习、深度学习（预测性维护）、生产调度优化四个章节，满足 "至少 3 章" 要求；
数据可行：公开数据集 + 合理模拟数据，不依赖企业内部数据；
可落地为 B/S demo：前后端 + 数据库 + 算法模块分层清晰，可在课程周期内完成可运行原型。
4.4 调研结论
单一技术方向（如纯视觉检测）难以满足 "至少 3 章技术" 的要求；将质量预测、预测性维护、智能排产整合到一个车间场景中，既能形成完整业务闭环，又能充分运用课程多章知识，是性价比最高的选题方案。