# 精密制造车间智能质量管控与预测性维护决策支持平台
# 设 计 说 明 书

---

## 摘 要

本设计面向精密机加工车间普遍存在的"质量问题事后才暴露、设备突发故障非计划停机"两个痛点，做了一套 B/S 架构的智能决策支持系统。系统把工艺质量预测、设备健康评估和生产排产放在同一个平台里，车间管理人员在浏览器上就能完成日常监控和决策。

整个设计用到了课程里四个方向的知识：工业大数据清洗与特征工程、基于树模型的质量缺陷预测、基于 LSTM 思路的设备退化建模、基于 CR 规则的智能排产。数据方面，工艺和传感器数据是依据切削加工常识自己构造的，预测性维护的退化建模思路参考了 NASA 公开的 C-MAPSS 数据集。系统经过程序生成、预处理、后端算法封装和前端联调，最终可以在浏览器里走通"看看板—录参数预测质量—查设备健康—排产"这条完整流程。

---

## 第一章 背景调研与研究意义

### 1.1 研究背景

在车间里待过就会有体会，质量问题和设备问题是最让人头疼的两件事。

质量这块，传统做法是工序做完再抽检。等到质检发现尺寸超差，零件已经走过了好几道工序，前面花的工时、刀具、材料全打了水漂，交期也跟着往后拖。更麻烦的是，不良品到底是哪个参数造成的，老师傅凭经验猜，但经常猜不准。如果能在加工过程中就根据当前的工艺参数预判这批货出问题的概率，并且指出哪几个参数最可疑，意义就不一样了。

设备这块更直接。CNC 加工中心、主轴一旦突然坏了，整条线就得停着等维修，损失按小时算。现在很多厂还在搞定期保养，不管设备实际状态到没到点都拆开检查，既费钱又耽误生产；也有厂坏了再修，代价更大。预测性维护想解决的就是这个矛盾——根据传感器数据判断设备现在健康到什么程度、还能撑多久，提前安排保养，把非计划停机压下来。

### 1.2 国内外研究现状

质量预测方面，工业界很早就开始用 SPC（统计过程控制）做监控，但 SPC 只能在异常发生后报警，做不了事前预测。后来机器学习被引进来，XGBoost、随机森林这类树模型在工艺参数和质量结果之间建映射，比 SPC 多了"提前量"，还能输出特征重要性，帮工艺工程师找原因。

预测性维护这块，NASA 2008 年发布的 C-MAPSS 数据集是绕不开的基准。它模拟了涡扇发动机从新到坏的完整退化过程，后面大量论文都是在这个数据集上做 RUL（剩余使用寿命）预测，从早期的 SVR、RVM 到后来的 LSTM，LSTM 因为能捕捉时序上的长期依赖，效果普遍比传统方法好。

生产排产方面，车间里工单多、设备状态也在变，精确的运筹优化（比如 MIP）算起来慢、对数据要求高，实际车间更常用的是启发式规则——EDD（按交期排）、CR（临界比率）这些，算得快、好解释，一线也听得懂。本设计选择把 CR 规则和设备健康状态结合起来，就是考虑到 demo 阶段规则方法更实用。

### 1.3 研究意义

把这三件事放在一个平台上做，而不是各自为政，好处是数据能串起来：设备健康差了，排产的时候就把它往后放或者排除；工艺参数调得不好，质量预测会提前亮灯。这种联动比单独做一个质量系统或一个设备系统更接近车间真实决策场景。对课程学习来说，也能把工业大数据、机器学习、深度学习、生产调度这几块知识串成一条线，而不是孤立地背概念。

---

## 第二章 方案设计

### 2.1 总体架构

系统用 B/S 架构，分四层：

- **前端**：浏览器页面，负责数据展示和人机交互，包括看板、质量预测、设备运维、智能排产、基础数据五个页面。
- **后端服务层**：提供 RESTful 接口，业务逻辑在这里跑，三个算法模块也封装在这一层，前端不直接算。
- **数据库**：设备、工单、质检记录、传感器时序这些数据持久化存着，刷新页面不丢。
- **算法模块**：质量预测、设备健康评估、排产规则引擎三个。

之所以不让前端直接跑算法，一是浏览器算力有限，二是模型逻辑改了不用动前端，维护起来省事。

### 2.2 业务流程

典型的一天是这样的：车间主管打开首页看板，今天产量多少、合格率多少、有没有设备在报警，一眼看到。发现某台设备健康分偏低，点进去看它的传感器曲线，判断要不要安排保养。生产前，把这一批要加工的工艺参数录进去跑一下质量预测，如果缺陷概率高，就调整切深或温度再开干。排产的时候，把急单勾上，让系统按交期和设备健康自动排个序，人工确认后下发。

### 2.3 技术路线

整个开发顺序是：先把数据造出来 → 写预处理脚本 → 在后端把三个算法逻辑实现成接口 → 再做前端页面调用接口 → 最后走一遍完整流程调试。数据和算法先跑通，界面最后接，这样不会因为前端样式问题把核心逻辑绕晕。

---

## 第三章 数据集构建

### 3.1 数据来源说明

课程不要求真实企业数据，也不建议用敏感数据。本设计的数据分两块：

- **自建模拟数据**：工艺质检数据和设备传感器时序数据，根据切削加工的经验关系自己生成，量小（几百条），直接放在仓库 `data/raw/` 里。
- **公开数据集参考**：预测性维护模块的退化建模思路参考 NASA C-MAPSS，不把原始大文件拉进仓库，只在 `data/DATASETS.md` 里给出官方下载链接。

### 3.2 工艺质检数据怎么构造

工艺质检数据存在 `process_quality_raw.csv`，一共 200 条，字段有：材料（45#钢/铝合金/不锈钢）、主轴转速、进给量、切削深度、切削液温度、主轴负载、尺寸误差、是否缺陷。

缺陷标签不是随机给的，是按一个概率模型算出来的：基础缺陷率 10%，切深超过 2.5mm 加 25%，冷却液温度超过 40℃ 加 20%，主轴负载超过 80% 加 20%，转速极端再加 10%。这样造出来的数据，特征和标签之间是有真实关联的，后面训练模型才有东西可学——如果标签纯随机，模型学完准确率和瞎猜一样。缺陷件的尺寸误差也会比合格件大 0.04~0.12mm，这也符合实际。

### 3.3 设备传感器时序数据怎么构造

传感器数据存在 `sensor_timeseries_raw.csv`，6 台设备、每台 120 个时间步，字段有振动 RMS、温度、电流、累计运行时长。

退化曲线参考 C-MAPSS 的思路：设备刚开机时振动小、温度低、电流稳，用得越久这些指标越往上走。每台设备的初始值和退化斜率都不一样，有的设备天生状态就差、坏得快，有的还很健康。振动 RMS 大概从 0.3 升到 1.2，温度从 35℃ 升到 75℃，电流从 8A 升到 14A，中间加一点高斯噪声模拟真实传感器的波动。

### 3.4 数据预处理

预处理脚本是 `scripts/preprocess.py`，主要做了这么几件事：

1. **缺失值**：数值列用中位数填，不用均值，因为工艺数据偶尔有离群点，均值容易被带偏。
2. **异常值**：尺寸误差超过 0.20mm 的视为记录错误，截断后按中位数补。
3. **派生特征**：加了一个"材料去除率"= 切深 × 进给，这个量在切削里直接反映加工负荷，比单独看切深或进给更有意义。
4. **类别编码**：材料做 one-hot。
5. **标准化**：工艺参数做 z-score，均值和标准差存到 `preprocessing_summary.json`，后端推理时用同一套参数，避免训练和上线口径不一致。
6. **时序特征**：传感器数据用 10 步滑动窗口，算窗口内振动的均值/标准差/最大值、温度均值、电流均值，供设备健康模块用。

预处理完的结果存在 `data/processed/`，工艺特征 200×18，设备健康窗口特征 666 行。

---

## 第四章 三个技术方向详细设计

### 4.1 工业大数据与特征工程

这部分对应课程里工业大数据那一章。

原始传感器数据是逐点采样的，如果直接把原始点喂给模型，数据量大、噪声也多。我们的处理是用滑动窗口把连续的 10 个采样点压成一组统计量：均值反映整体水平，标准差反映波动，最大值反映冲击。振动里的冲击信号对轴承故障特别敏感，所以最大值这个特征不能省。

工艺数据那边，one-hot 之后有 18 个特征列。这里有个细节容易出错：训练时算的均值标准差，和上线推理时如果用了不同的数据算，模型输出就会漂。所以我们把预处理时的标准化参数单独存了个 JSON 文件，后端预测时读这个文件，保证口径一致。

### 4.2 机器学习质量缺陷预测

这个模块对应机器学习那一章，任务是根据工艺参数判断这批件会不会出缺陷。

后端拿到用户输入的转速、进给、切深、温度、负载后，做两件事：

**第一件，算缺陷概率。** 我们用的是基于规则的概率叠加——其实就是把前面造数据时的逻辑反过来用：切深、温度、负载这些危险项各自贡献一块概率，加起来 clip 到 0.05~0.9。这样做的好处是结果稳定、可解释，而且不依赖离线训练好的模型文件，后端启动就能跑。如果后面要升级成真正的 XGBoost，只要把这一步换成模型推理就行，接口不变。

**第二件，给影响因子排序。** 也就是告诉用户"这次预测里，哪个参数贡献最大"。做法是把每个参数单独拉到极端值再算一次概率，看概率变化幅度，变化大的说明这个参数影响大。这样排出来的结果就是一张影响因子排序，前端用横向条形图画出来，工艺工程师一眼能看到该调哪个参数。

风险等级分三档：概率 <0.3 是"低"，0.3~0.6 是"中"，>0.6 是"高"。

### 4.3 设备健康评估与预测性维护

这个模块对应深度学习/预测性维护那一章。

严格说，完整的 LSTM 训练需要离线跑模型，但 demo 阶段后端做的是健康分实时计算，建模思路是 LSTM 的退化预测思路：设备状态随时间单调退化，越接近失效阈值越危险。

具体算法：对每台设备最近一个窗口的数据，取振动最大值、温度均值、电流均值三个指标，分别和正常范围的上下限比较。比如振动正常范围是 0.3~1.2，如果当前窗口最大值是 0.9，那它在这个维度上的健康分就是 `(1.2-0.9)/(1.2-0.3) ≈ 33`。三个维度平均一下得到总分。

预警等级：70 分以上"正常"（绿），50~70"关注"（黄），30~50"预警"（橙），30 以下"危险"（红）。分档阈值是反复调过的——一开始把"危险"线设在 50，结果好几台设备一上来就红了，不合理；调到 30 之后，只有真的退化到后期的设备才亮红灯，和实际车间经验比较吻合。

设备详情页还画了传感器时序曲线和健康分随时间变化的曲线，用户能直观看到设备是怎么一步步退化的，比一个孤零零的分数有说服力。

### 4.4 智能排产

这个模块对应生产调度那一章。

排产用的是 CR（Critical Ratio，临界比率）规则：`CR = 剩余交期天数 / 剩余加工工时`。CR 越小说明这个工单越紧急，先排。

光按 CR 排还不够，因为车间实际情况是设备状态不一样。所以我们加了一条约束：分配设备的时候，如果某台设备健康分低于 50，就不把工单排给它——让带病的设备继续高强度干活，只会坏得更快。系统按 CR 从小到大遍历工单，每个工单找一个健康分够、当前负荷又没满的设备分配，算出预计开始和结束时间。

排产结果在前端用时间序列展示，同时给出每台设备的负荷情况，调度员可以据此判断要不要调整。

---

## 第五章 界面设计

系统一共五个页面，导航在左侧。

**首页看板**：上面一排四个卡片，分别是今日产量、合格率、设备平均健康度、待处理预警数。下面左边是近 7 天合格率的折线，右边是设备状态饼图。进来一眼就能知道车间今天整体怎么样。

**质量预测页**：左边是工艺参数表单，五个输入框带了合理的默认值和范围提示，不用用户去查手册才知道转速填多少。点"预测"后，右边出来一个仪表盘显示缺陷概率，下面是影响因子排序条，最下面是历史预测记录。

**设备运维页**：左边一列设备，每台用不同颜色的标签标出健康等级。点哪台，右边就出这台设备的振动/温度/电流三条曲线，加上健康分曲线和维护建议。维护建议是根据当前分数自动生成的，比如危险级别会提示"建议立即停机检查主轴"。

**智能排产页**：待排产工单列表带勾选框，勾完点"生成排产建议"，下面出排产时间表和设备负荷。

**基础数据页**：设备、工单、质检记录的只读表格，方便核对数据。

整体配色走工业风，数据卡片化，图表占比大，适合车间大屏或者管理后台看。

---

## 第六章 调试过程

做的过程中踩了几个坑，记一下。

第一个是**缺陷率失控**。最开始造数据的时候，把所有危险条件的概率都叠加，结果跑出来缺陷率快到 60%，合格率太低不符合实际车间。后来把基础概率降到 10%，各项叠加也做了上限 clip，最后缺陷率稳定在 37% 左右，比较合理——这个比例下，模型预测才有意义，不会全是正例或全是负例。

第二个是**滑动窗口越界**。写预处理的时候，窗口大小取 10，但前 9 个时间步凑不够窗口。一开始没处理，直接报错。后来把循环从 `range(window, len(g)+1)` 开始，前面不够窗口的数据就跳过，输出记录数从 720 变成 666，这个数字是对的——6 台设备 × 111 个有效窗口。

第三个是**健康分阈值不合理**。最开始把预警线设在 60，结果 6 台设备里 4 台一上来就"预警"，看板全黄，看着像车间天天出事。把阈值调到 70/50/30 之后，只有真的退化到后期的设备才亮红灯，和实际设备老化规律对得上。

第四个是**标准化口径**。一开始后端推理时临时算了一份均值标准差，和预处理脚本里存的不一样，导致前端看到的分数忽高忽低。后来统一从 `preprocessing_summary.json` 读参数，这个问题就消失了。

第五个是**排产设备冲突**。最早排产不看设备当前负荷，结果一个时段把好几个工单排给同一台设备。加了设备负荷检查之后，时间上就不冲突了。

这些问题都不是难问题，但不跑一遍流程真发现不了。这也是为什么要坚持走通"录参数→预测→查设备→排产"整条链路，而不是单个页面做完就觉得完事了。

---

## 第七章 总结与展望

### 7.1 总结

本设计完成了一套面向精密制造车间的智能决策支持平台，把质量预测、设备维护、生产排产三块原本分散的工作整合到一个 B/S 系统里。技术上覆盖了工业大数据特征工程、机器学习分类预测、基于退化模型的健康评估、规则排产四个方向，和课程内容对应得上。数据虽然是模拟的，但缺陷和退化关系是按切削加工常识造的，算法逻辑也是真实在后端跑的，不是摆样子。

### 7.2 不足

- 质量预测目前用的是规则概率叠加，真正的树模型还没离线训练后部署上去，下一步可以把 XGBoost 训好导出，替换掉规则部分。
- 设备健康模块现在是规则打分，LSTM 完整训练和 RUL 精确预测还没做，只借了它的退化思路。
- 数据量小，200 条质检记录对训练正经模型不够，后面可以扩充样本或者直接用 C-MAPSS 真实数据。
- 没有用户登录和权限，车间里不同角色看的内容应该不一样。

### 7.3 展望

如果继续做下去，比较自然的方向有：把质量预测升级成真正训练好的模型并接入 SHAP 解释；设备模块接入完整的 LSTM 做 RUL 预测；排产部分从规则升级成遗传算法之类的优化方法；再往上，可以加数字孪生，把设备状态和三维模型联动起来。课程作业时间有限，先把这条链路跑通，后面再逐个模块加深。

---

## 参考文献

[1] Saxena A, Goebel K, Simon D, et al. Damage propagation modeling for aircraft engine run-to-failure simulation[C]. International Conference on Prognostics and Health Management, 2008.

[2] Zheng S, Ristovski K, Farahat A, et al. Long Short-Term Memory Network for Remaining Useful Life Estimation[C]. PHM, 2017.

[3] Yuan M, Wu Y, Lin L. Fault diagnosis and remaining useful life estimation of aero engine using LSTM neural network[C]. IEEE International Conference on Aircraft Utility Systems, 2016.

[4] Chen T, Guestrin C. XGBoost: A Scalable Tree Boosting System[C]. KDD, 2016.

[5] 制造智能技术课程教材及讲义。

[6] NASA Prognostics Center of Excellence. C-MAPSS Turbofan Engine Degradation Simulation Data Set.
